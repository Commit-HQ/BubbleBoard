// App pages are prerendered shells that run in the browser. Their HTML holds no classroom data: that is
// fetched and decrypted on the device, never rendered by the server (docs/access-format.md).
export const prerender = true;
