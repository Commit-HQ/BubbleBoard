// The one landing page that runs in the browser: moving between steps without the page jumping, and with the
// arrow keys, takes a little script. Without it the steps' links still work, as links to each step.
export const csr = true;
